interface:
  display_name: "Optimize GPT-5.6 Prompts"
  short_description: "Create lean, task-ready prompts for GPT-5.6"
  default_prompt: "Use $optimize-gpt-5-6-prompts to improve the following prompt for GPT-5.6 without executing it: [prompt]"

policy:
  allow_implicit_invocation: false
