# {{接口名称}}

> 文档状态：{{已确认 / 当前实现 / 已验证 / 待确认}}
>
> 适用调用方：{{Web / 移动端 / 管理后台 / 第三方系统}}

## 1. 接口摘要

{{用一句话说明资源、范围和返回结果。}}

## 2. 请求定义

| 项目 | 内容 |
|---|---|
| Method | `{{GET}}` |
| Path | `{{/api/example}}` |
| Base URL | `{{按环境填写或引用全局约定}}` |
| Authentication | `{{Bearer Token / 无 / 其他}}` |
| Permission | `{{资源级访问条件}}` |
| Content-Type | `{{application/json / multipart/form-data / 不适用}}` |

## 3. 请求参数

| 参数 | 位置 | 类型 | 必填 | 可为 null | 默认值 | 业务含义 | 约束 |
|---|---|---|---:|---:|---|---|---|
| `{{field}}` | `{{query/path/header/body}}` | `{{type}}` | `{{是/否}}` | `{{是/否}}` | `{{value/—}}` | `{{稳定的业务定义}}` | `{{格式、范围、匹配规则}}` |

### 请求示例

```http
{{METHOD}} {{PATH}}?{{query}}
Authorization: Bearer <redacted-token>
Content-Type: application/json
```

```json
{
  "{{field}}": "{{fictional-value}}"
}
```

## 4. 成功响应 Schema

```json
{
  "code": 200,
  "msg": "success",
  "data": {},
  "total": null
}
```

> 示例数据为虚构或脱敏数据；必须与下方字段规则和业务不变量一致。

## 5. 响应字段字典

| 字段 | 类型 | 始终存在 | 可为 null | 业务含义 | 取值/关系 |
|---|---|---:|---:|---|---|
| `{{data.field}}` | `{{type}}` | `{{是/否}}` | `{{是/否}}` | `{{定义}}` | `{{范围、单位、排序或公式}}` |

## 6. 业务规则与不变量

- {{精确匹配、过滤、去重、排序或时间窗口规则。}}
- {{统计公式、分子、分母、单位和精度。}}
- {{字段之间必须成立的关系。}}

## 7. 空数据与边界行为

| 场景 | HTTP/code | 响应含义 |
|---|---:|---|
| `{{资源不存在}}` | `{{404}}` | `{{说明}}` |
| `{{资源存在但无匹配数据}}` | `{{200}}` | `{{空数组、null 或其他结构}}` |
| `{{参数不合法}}` | `{{400}}` | `{{说明}}` |

## 8. 错误响应

| HTTP | code | 触发条件 | 调用方可依赖的处理依据 |
|---:|---:|---|---|
| `{{401}}` | `{{401}}` | `{{未认证}}` | `{{状态码或稳定错误码}}` |
| `{{403}}` | `{{403}}` | `{{无资源权限}}` | `{{状态码或稳定错误码}}` |
| `{{500}}` | `{{500}}` | `{{内部错误}}` | `{{不要依赖具体 message}}` |

## 9. 契约依据与待确认项

### 已确认

- {{已明确的对外规则。}}

### 当前实现

- {{代码当前表现，但尚未确认是否是长期保证的行为。}}

### 已验证

- {{验证环境、场景和结果；不记录凭据或真实个人数据。}}

### 待确认

- {{缺失的业务决策或存在冲突的规则。}}

## 10. 变更记录

| 版本/日期 | 变更 | 兼容性影响 |
|---|---|---|
| `{{v1 / YYYY-MM-DD}}` | `{{initial / field change}}` | `{{none / breaking / migration}}` |
