---
title: "{{中文标题}}"
aliases:
  - "{{英文术语}}"
tags:
  - cs/{{领域英文标识}}
type: concept
created: "{{YYYY-MM-DD}}"
updated: "{{YYYY-MM-DD}}"
---

# {{中文标题}}

%% 一篇笔记只围绕一个中心主题。概念笔记默认保持在约 800–1800 个中文字符（代码除外）。删除不适用的章节，不要把所有相关知识都塞进同一篇笔记。 %%

> [!abstract] 核心结论
> 用一到三句话直接说明最重要的结论。

## 核心概念

说明要解决的问题，并给出准确术语、定义和基本模型。核心术语首次引入时，使用“英文正式术语（中文规范译名：准确、简炼地定义它是什么）”；如果概念有自然的代码、类型签名、命令、数据结构、伪代码或状态表示，优先在首次定义附近给出最小真实表达。

例如：``type predicate（类型谓词：函数返回类型中形如 `value is T` 的类型标注，表示函数返回 `true` 时，TypeScript 将 `value` 视为 `T`。）``。

首次定义只建立英文名、中文名、准确“是什么”定义和真实表达；动机、机制、边界和验证在后文展开。完成首次定义后，后续默认使用中文名，必要时使用“中文名（English term）”。

对于概念比较，优先使用一张比较表；不要为每个术语分别写一篇长论文。

只在确实有助于理解时加入一个简短直觉场景，并立即映射回专业概念和边界。

## 核心概念地图

```text
概念 A -> 关系 -> 概念 B
```

先列出本文必须解释的主概念和关系。核心术语在地图或正文首次解释时使用英文正式术语（中文规范译名：准确、简炼地定义它是什么）；有自然表达时，附上最小真实形态。地图用于暴露学习缺口，不能代替正文定义。

## 最小前置知识

只回顾理解当前机制不可缺少的内容。对有基础读者可写“快速回顾”，但不要省略主概念的定义、动机和边界。

## 主概念剖析

### {{英文正式术语}}（{{中文规范译名}}：{{准确、简炼地定义它是什么；有自然表达时给出最小真实形态}}）

#### 它是什么

#### 为什么需要它

#### 它在当前机制中的作用

#### 它不是什么

## 运行机制

按照时间、状态变化、数据流、控制流或推导顺序解释核心过程。

如果当前主题是概念比较，可以将本节替换为“关键区别”；如果是算法，可以替换为“算法步骤与复杂度”。

## 完整示例

> [!info] 示例环境
> - 语言、数据库或工具：
> - 方言或版本：
> - 结果状态：预期结果 / 实际运行结果

提供一个最小、完整、可复现的代表性示例。按需要包含数据准备、代码或命令、运行结果和逐步解释。

```text
使用正确的语言标识替换 text。
```

不要只贴代码；解释输入、关键语句、状态变化和结果原因。

## 边界与工程注意

> [!warning] 常见误区
> 说明一到三个最重要的错误理解、边界情况或失败模式。

> [!tip] 工程注意
> **建议**：规范写法或推荐做法。  
> **原因**：为什么这样做。  
> **不这样做可能出现的问题**：具体后果。

没有实际工程决策时删除工程提示块。

## 小结

- 核心定义：
- 关键机制：
- 重要边界：

## 自检问题

1. 给出一个需要读者预测行为或解释机制的问题。

> [!success]- 参考答案
> 指出结论依赖的具体概念和推理过程，不只重复结论。

## 进阶方向

- 只列出值得后续单独学习的主题，不在本篇展开。

没有明确进阶分支时删除本节。

## 相关笔记

- 前置：[[前置知识]]
- 相关：[[相关概念]]
- 后续：[[后续知识]]

只保留真实存在或已经规划的链接。

## 参考资料

- 一到三个真正相关的官方文档、标准、权威教材或经典资料。
